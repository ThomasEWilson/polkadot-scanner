// import type { BlockNumber } from '@polkadot/types/interfaces';

// import { useApi } from './useApi';

export function useBestNumber (): number {
//   const { api } = useApi();
    return 5;
//   return useCall<BlockNumber>(api.derive.chain.bestNumber);
}